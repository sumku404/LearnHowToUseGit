// Proxy by sumkux 2021

let toggle = false;

let routeMapping = [];

function mount() {
	toggle = true;
}

function unmount() {
	toggle = false;
	routeMapping = [];
}

function route(destination, url) {
	// do somthing...
}
